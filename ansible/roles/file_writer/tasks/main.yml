- name: packages are installed
  unarchive:
    remote_src: yes
    src: https://jenkins.esss.dk/dm/job/ess-dmsc/job/kafka-to-nexus/job/jenkins/lastSuccessfulBuild/artifact/file-writer.tar.gz
    dest: "~"

- name: file writer start command script is installed
  copy:
    src: "send-start-cmd.py"
    dest: "~"
    mode: 0755

- name: file writer start command script is installed
  copy:
    src: "send-stop-cmd.py"
    dest: "~"
    mode: 0755
